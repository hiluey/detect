import { Metadata } from "next"

export const metadata: Metadata = {
  title: 'AI-Detect History',
  description: 'Detector de IA'
}

export default function History() {
    return(
      <div>
        <h1>History</h1>
      </div>
    )
  }