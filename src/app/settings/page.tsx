import { Metadata } from "next"

export const metadata: Metadata = {
  title: 'AI-Detect Settings',
  description: 'Detector de IA'
}
export default function Set() {
    return(
      <div>
        <h1>Settings</h1>
      </div>
    )
  }